import { IdConvertPipe } from './id-convert.pipe';

describe('IdConvertPipe', () => {
  it('create an instance', () => {
    const pipe = new IdConvertPipe();
    expect(pipe).toBeTruthy();
  });
});
