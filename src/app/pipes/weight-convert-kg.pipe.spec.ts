import { WeightConvertKgPipe } from './weight-convert-kg.pipe';

describe('WeightConvertKgPipe', () => {
  it('create an instance', () => {
    const pipe = new WeightConvertKgPipe();
    expect(pipe).toBeTruthy();
  });
});
