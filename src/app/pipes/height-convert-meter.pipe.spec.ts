import { HeightConvertMeterPipe } from './height-convert-meter.pipe';

describe('HeightConvertMeterPipe', () => {
  it('create an instance', () => {
    const pipe = new HeightConvertMeterPipe();
    expect(pipe).toBeTruthy();
  });
});
